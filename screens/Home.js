import React from 'react';
import {
    View,
    Text,
    TouchableOpacity,
    ImageBackground,
    StatusBar
} from 'react-native';

import { image, COLORS, SIZES } from '../constants';

const Home = ({ navigation }) => {
    return (
        <View

        >

        </View>
    )
}

export default Home;